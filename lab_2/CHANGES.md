# CHANGES

## Лабораторная работа 2

- Старый DAG заменен на новый DAG с `SparkSubmitOperator`.
- Добавлен `spark-master`.
- Добавлен `spark-worker`.
- Добавлен spark-job на `pyspark`.
- Обновлены `Dockerfile`, `docker-compose.yml`, `README.md` и `AGENTS.md`.
