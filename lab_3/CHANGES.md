# Изменения в ЛР3 относительно ЛР2

- База `Airflow + Spark` перенесена из `lab_2` в отдельную директорию `lab_3`.
- Для третьей лабораторной добавлен корневой workflow `.github/workflows/lab_3.yml`.
- CI/CD-часть задания адаптирована с `GitLab CI` на `GitHub Actions`.
- Локальная документация `lab_3/README.md` переписана под сценарий `GitHub Actions` и `deploy` через `docker compose up -d`.
